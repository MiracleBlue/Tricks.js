define(function(require) {
	return function() {
		// Bouncy!
		// The way to access this animation system is to get the modifier like so
		// var bounceIn = Tricks.bounceIn();
		// myViewObject.add(bounceIn.modifier).add(mySurface);
		// bounceIn.full();
	}
});